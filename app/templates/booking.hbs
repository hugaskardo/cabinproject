{{outlet}}

{{#each model as |booking|}}
  <ul>
    <li>{{booking.client}}</li>
    <li>{{booking.checkIn}}</li>
    <li>{{booking.checkOut}}</li>
    <li>{{booking.days}}</li>
    <li>${{booking.price}}</li>
  </ul>
{{/each}}

<!-- div class="container">
  <p>Check-in</p> {{bootstrap-datepicker value=expiresAt}}
  <p>Check-out</p> {{bootstrap-datepicker value=expiresAt}}
</div -->

<div class="container">
  <div class="row">
    <div class="col-md-6">
      <h4>Check In</h4> {{calendar-1}}
    </div>
    <div class="col-md-6">
      <h4>Check Out</h4> {{calendar-1}}
    </div>
  </div>
</div>
